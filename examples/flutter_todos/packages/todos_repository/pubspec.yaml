name: todos_repository
description: A repository that handles todo related requests.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  todos_api:
    path: ../todos_api

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.21.4
