/// [AuthenticationStatusType] define users auth status
enum AuthenticationStatusType {
  /// unknown status
  unknown,

  /// authenticated status
  authenticated,

  /// unauthenticated status
  unauthenticated,
}
