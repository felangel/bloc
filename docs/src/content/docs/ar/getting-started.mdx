import 'package:flutter/material.dart';

void main() {
  runApp(SamyTelecomApp());
}

class SamyTelecomApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Samy Telecom',
      debugShowCheckedModeBanner: false,
      home: HomePage(),
    );
  }
}

class HomePage extends StatelessWidget {
  Widget buildCard(String title, IconData icon) {
    return Card(
      child: Padding(
        padding: EdgeInsets.all(20),
        child: Column(
          children: [
            Icon(icon, size: 40, color: Colors.red),
            SizedBox(height: 10),
            Text(title),
          ],
        ),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Samy Telecom"),
        backgroundColor: Colors.red,
      ),
      body: Column(
        children: [
          SizedBox(height: 20),
          Text("سامي تليكوم",
              style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold)),

          SizedBox(height: 20),

          Row(
            mainAxisAlignment: MainAxisAlignment.spaceAround,
            children: [
              buildCard("شحن رصيد", Icons.phone_android),
              buildCard("باقات", Icons.wifi),
            ],
          ),

          Row(
            mainAxisAlignment: MainAxisAlignment.spaceAround,
            children: [
              buildCard("كروت", Icons.credit_card),
              buildCard("تحويل", Icons.swap_horiz),
            ],
          ),

          SizedBox(height: 20),

          Text("771751444",
              style: TextStyle(fontSize: 20, color: Colors.red)),
        ],
      ),
    );
  }
}
